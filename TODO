To do:

1) Information on resource, Name picture description etc: ACTIONING

2) Block creation of events before "today": DONE

3) Users view their own requests - probably via user panel

4) As part of {1}, split app config into default and user configurable files: ACTIONING

5) Admin can delete requests

Extension:

1) Setup util, to set various things in userSettings